{
  "name": "@ads-bazaar/contracts",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "cargo build --target wasm32-unknown-unknown --release",
    "test": "cargo test",
    "fmt": "cargo fmt --all",
    "deploy:testnet": "stellar contract deploy --wasm target/wasm32-unknown-unknown/release/ads_bazaar_contracts.wasm --network testnet"
  }
}
